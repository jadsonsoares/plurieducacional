import { LessonSearchPipe } from './lesson-search.pipe';

describe('LessonSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LessonSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
